<p>Enter the IP address the browser autopwn should be started.<br/>
The website with browser autopwn will be available at http://&lt;your IP address&gt;:8080/</p>
<% form_tag startbrowserautopwn_browser_autopwn_url do |f| %>
  <% if @interfaces.empty?%>
    <%= text_field_tag :address %>
  <% else %>
    <%= select_tag('adress', options_for_select(@interfaces)) %>
  <% end %>
  <%= submit_tag :'Start Browser autopwn' %>
<% end %>

<p>Enter the IP address the file autopwn should be bind to.<br/>
The file autopwn will build a web page at http://&lt;your IP address&gt;:8080/file with loots of links to funny documents.</p>
<% form_tag startfileautopwn_browser_autopwn_url do |f| %>
  <% if @interfaces.empty?%>
    <%= text_field_tag :address %>
  <% else %>
    <%= select_tag('adress', options_for_select(@interfaces)) %>
  <% end %>
  <%= submit_tag :'Start File autopwn' %>
<% end %>
